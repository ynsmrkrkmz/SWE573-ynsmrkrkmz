import styled from '@emotion/styled';

const Root = styled.div`
  width: 100%;
  height: calc(100vh - 68px);
  display: flex;
  justify-content: top;
  align-items: top;
  overflow: overlay;
`;

export default Root;
